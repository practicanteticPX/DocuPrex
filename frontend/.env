# URL del API Backend
# NOTA: Esta variable NO se usa directamente en el código
# El código usa getBackendHost() que detecta automáticamente el protocolo (HTTP/HTTPS)
# y construye la URL dinámicamente según el hostname y protocolo de acceso
#
# - Si acceden por HTTPS (ej: https://docuprex.com), usa rutas relativas + proxy de Vite
# - Si acceden por HTTP (ej: http://192.168.0.19:5173), usa URL absoluta con puerto 5001
#
# Esta variable se mantiene solo como referencia/documentación
VITE_API_URL=http://docuprex.com:5001/graphql
